# HO別 機材・配線まとめ

**対象**: M5Stack Core2 for AWS
**用途**: 実機検証時の準備確認、当日の配布物チェック、参加者向け早見表

---

## 0. 一覧表

| | ハンズオン | 追加で繋ぐもの | 使うポート | 配線図 |
|---|---|---|---|---|
| HO-0 | 自己診断 | すべて | 全部 | 図1〜3 |
| HO-1 | Lチカ | **なし**（内蔵LED） | G25（内部） | **図0** |
| HO-2 | 温湿度センサー | ENV Ⅲ | PORT.A | **図1** |
| HO-3 | サーボ | ブレッドボード一式 + SG92R | PORT.B | **図2** |
| HO-4 | ブラウザ連携 | HO-2 + HO-3 を同時に | PORT.A + PORT.B | **図3** |
| HO-5 | AI 組み込み | **なし**（HO-4 と同じ） | 同上 | 図3 |
| HO-6 | Pan-Tilt | Pan-Tilt + 外部電源 | **PORT.C**（独立バス） | **図5** |

**HO-1 と HO-5 は追加の配線がありません。** HO-1 は内蔵 LED、HO-5 は HO-4 の配線のままソフトだけ変わります。

---

## 1. 機材リスト（1人分）

### 全 HO で共通

| 品目 | 数 | 備考 |
|---|---|---|
| M5Stack Core2 for AWS | 1 | ベース（M5GO-Bottom For AWS）付き |
| USB-C ケーブル | 1 | **データ通信対応品。充電専用は不可** |
| PC | 1 | Chrome 148以上、RAM 16GB以上 |
| セルフパワー USBハブ | 4人に1台 | 電源安定のため推奨 |

### HO-2 以降

| 品目 | 数 | 備考 |
|---|---|---|
| ENV Ⅲ Unit | 1 | SHT30（0x44）+ QMP6988（0x70） |
| Grove ケーブル | 1 | ENV Ⅲ 付属 |

### HO-3 以降

| 品目 | 数 | 備考 |
|---|---|---|
| ブレッドボード（ハーフ） | 1 | **運営が事前に組立・動作確認** |
| SG92R マイクロサーボ | 1 | |
| USB AC アダプタ 5V/2A | 1 | **モバイルバッテリー不可** |
| USB電源取り出し基板 | 1 | |
| 電解コンデンサ 1000µF/16V | 1 | 極性注意 |
| カーボン抵抗 330Ω | 1 | GPIO保護用 |
| ピンヘッダ 3ピン | 1 | サーボを受ける |
| Grove→ジャンパ変換ケーブル | 1 | **赤線は運営が切断・絶縁済み** |
| ジャンパワイヤ（オス-オス） | 数本 | |

### HO-6（全体で1セットのみ・運営デモ用）

| 品目 | 数 | 備考 |
|---|---|---|
| Arducam Pan-Tilt キット | 1 | PCA9685系（0x40）+ サーボ2個 |
| I2Cレベルシフタ（双方向） | 1 | **バス電圧を実測してから要否を判断** |
| 電解コンデンサ 470〜1000µF | 1 | Pan-Tilt 電源用 |
| USB AC アダプタ | 1 | サーボ電源 |

---

## 2. ポート早見表

| ポート | 場所 | ピン | 何を繋ぐか |
|---|---|---|---|
| **PORT.A**（赤） | 本体上部 | G32 = SDA / G33 = SCL | ENV Ⅲ（**I2C バス0**） |
| **PORT.B**（黒） | ベース側面 | **G26** / G36（入力専用） | サーボの PWM 信号 |
| **PORT.C**（青） | ベース側面 | G14 = SDA / G13 = SCL | Pan-Tilt（**I2C バス1**） |
| LED バー | ベース内部 | **G25** | 配線不要 |

**使えないピン**: GPIO16 / GPIO17（PSRAM が占有）

---

## 3. HO-1　Lチカ

### 配線

**ありません。** USB ケーブルで PC と繋ぐだけです。

→ **図0** を参照

### 押さえる点

LED バーはベース側面に内蔵されていて、データ線は **G25** です。PORT.A や PORT.B とは競合しません。

**明るさは 0〜100 のパーセント指定で、12% 程度で十分です。** 白（`0xffffff`）で 10 個を 100% 点灯すると約 600mA になり、Core2 の入力仕様（5V @ 500mA）を超えます。

```python
rgb25 = RGB(io=25, n=10, type="SK6812")
rgb25.set_brightness(12)
```

---

## 4. HO-2　温湿度センサー

### 配線

```
Core2 PORT.A ──Groveケーブル──→ ENV Ⅲ Unit
（本体上部・赤）
```

Grove ケーブルを挿すだけです。向きは物理的に決まっているので、間違えようがありません。

→ **図1** を参照

### 確認方法

```python
i2c = I2C(0, scl=Pin(33), sda=Pin(32), freq=100000)
print([hex(a) for a in i2c.scan()])
```

- `0x44` = SHT30（温湿度）
- `0x70` = QMP6988（気圧）

**`[]` が返ったら PORT.B に挿していないか確認してください。**

---

## 5. HO-3　サーボ

**ここだけ配線が複雑です。** ブレッドボードは運営が事前に組み立てて配ります。

### 配線

```
                   ┌──────── ブレッドボード（組立済） ────────┐
USB ACアダプタ ────┼─ ＋5V ─┬──────────────────────────────┼→ サーボ 赤
                   │        │                              │
                   │   電解コンデンサ 1000µF                │
                   │        │                              │
Core2 PORT.B ──────┼─ 330Ω ─┼──────────────────────────────┼→ サーボ 橙（信号）
  1番ピン (G26)    │        │                              │
Core2 PORT.B ──────┼─ GND ──┴──────────────────────────────┼→ サーボ 茶
  4番ピン (GND)    │                                        │
                   └────────────────────────────────────────┘
```

→ **図2** を参照

### 参加者がやること（2手のみ）

1. サーボの3ピンをブレッドボードのヘッダに挿す（**橙 赤 茶** の並び）
2. Grove 変換ケーブルを **PORT.B** に挿す

**通電は最後**です。配線を済ませてから AC アダプタをコンセントに挿します。

### 絶対に守ること

| | 理由 |
|---|---|
| **Grove の赤線を使わない** | M5Stack の5Vと外部電源が繋がり、再起動する |
| **GND は必ず共通にする** | 基準電位が定まらず、サーボが暴れる |
| ホーンには紙の指示針だけ | 指を挟む・机から落ちる事故を防ぐ |

Grove 変換ケーブルの赤線は、**運営が事前に根元で切断し熱収縮チューブで絶縁**しておきます。

---

## 6. HO-4 / HO-5　ブラウザ連携とAI

### 配線

HO-2 と HO-3 の配線を**同時に**行います。追加の部品はありません。

```
PC ──USB──→ Core2 ──PORT.A──→ ENV Ⅲ
                    └─PORT.B──→ ブレッドボード ──→ サーボ
                                      ↑
                              USB ACアダプタ
```

→ **図3** を参照

**PORT.A と PORT.B は別系統なので、Grove ハブは不要です。**

### HO-5 で追加するもの

**ありません。** 配線は HO-4 のまま、ブラウザ側のコードだけが変わります。

### 通信の約束

```
ボード → PC:  {"t":25.3,"h":48.1,"btn":0}
PC → ボード:  {"servo":120,"status":"HUMID","msg":"換気しましょう"}
```

### 注意

**IDE とブラウザページは同時に接続できません。** シリアルポートは1つのアプリしか掴めないため、IDE の「Disconnect」を押してからページの「接続」を押します。

---

## 7. HO-6　Pan-Tilt（運営デモ / 3日版のみ）

### 配線（Grove ハブは使いません）

**ENV Ⅲ と Pan-Tilt を、別々のポートに独立して繋ぎます。**

```
Core2 PORT.A ──Groveケーブル──→ ENV Ⅲ           … I2C バス0
（赤・本体上部）  G32=SDA / G33=SCL

Core2 PORT.C ──Groveケーブル──→ Pan-Tilt 制御基板 … I2C バス1
（青・ベース側面） G14=SDA / G13=SCL    ※赤線は使わない
                                          │
          USB ACアダプタ ── 4.8〜5V ───────┘  ← サーボ電源
               GND は Grove の黒線経由で Core2 と共通
```

→ **図5** を参照

### なぜ2つのポートに分けられるのか

ESP32 には **I2C コントローラが2系統**あります。`I2C(0)` と `I2C(1)` を別々のピンに割り当てれば、2本の独立したバスとして同時に使えます。

```python
i2c_env = I2C(0, scl=Pin(33), sda=Pin(32), freq=100000)   # PORT.A
i2c_pca = I2C(1, scl=Pin(13), sda=Pin(14), freq=100000)   # PORT.C
```

**PORT.B は I2C に使えません。** G36 が入力専用ピンで、双方向通信が必要な SDA を割り当てられないためです。したがって2つ目の I2C は PORT.C 一択になります。

### バスを分ける利点

**0x70 のアドレス衝突が原理的に起きなくなります。**

| デバイス | アドレス | バス |
|---|---|---|
| SHT30（温湿度） | 0x44 | バス0 |
| QMP6988（気圧） | **0x70** | バス0 |
| PCA9685 | 0x40 | バス1 |
| PCA9685 の All Call | **0x70** | バス1 |

同じ 0x70 が2つありますが、**別のバスなので出会いません。** 「同じ番地の家が2軒あっても、通りが違えば郵便は届く」ということです。

ALLCALL の無効化は、この構成では必須ではありません。ただし将来このバスに別のデバイスを足す可能性と、`scan()` の結果が紛らわしくなるのを避けるため、コードには残してあります。

### 3つのポートを同時に使える

| ポート | 用途 |
|---|---|
| PORT.A | ENV Ⅲ（温湿度） |
| PORT.B | SG92R（1軸サーボ・HO-3） |
| PORT.C | Pan-Tilt（2軸サーボ） |

互いに干渉しないので、**全部同時に動かせます。**

### 繋ぐ前に必ず測ること

**PORT.C に繋ぐ前に、Pan-Tilt 基板側の SDA / SCL のアイドル電圧をテスタで測定してください。**

| 実測値 | 対応 |
|---|---|
| 3.3V | そのまま繋いでよい |
| 5V | **I2Cレベルシフタが必須**（ESP32 は5Vトレラントではない） |

判断に迷うなら、最初からレベルシフタを入れるのが安全です。

> **PORT.C に5Vのバスを直結すると、Core2 の I2C が壊れる可能性があります。** バス0（PORT.A）まで巻き添えになると、センサーも読めなくなります。順番を守ってください。

### PORT.C のピン割り当ては実機で確認

図とコードでは **黄（1番ピン）= G14 = SDA、白（2番ピン）= G13 = SCL** を想定しています。PORT.A が「黄 = SDA」なので同じ規則のはずですが、実機で `scan()` に何も出ない場合は入れ替えて試してください。

```python
# 逆だった場合
i2c_pca = I2C(1, scl=Pin(14), sda=Pin(13), freq=100000)
```

### コード側で必ずやること

```python
pca.set_freq(50)       # ★初期状態は50Hzではない★
```

### 可動範囲のリミット

ブラケットの構造上、180度全域は回りません。**範囲外の指令を出し続けるとギヤが傷みます。**

```python
PAN_LIMIT  = (30, 150)     # 実機で確認した値に
TILT_LIMIT = (70, 130)
```

### 付属カメラについて

Pan-Tilt キットのカメラは **Raspberry Pi の CSI 接続用**で、Core2 では使えません。画像を扱いたい場合は PC の Web カメラと Prompt API のマルチモーダル入力を使ってください。

---

## 8. 配線図ファイル一覧

| 図 | 内容 | ファイル |
|---|---|---|
| 図0 | LED バーの位置（HO-1） | `fig0_ledbar.svg` |
| 図1 | センサー接続（HO-2） | `fig1_sensor.svg` |
| 図2 | サーボ外部電源（HO-3） | `fig2_servo_wiring.svg` |
| 図3 | システム全体（HO-4 / HO-5） | `fig3_system.svg` |
| 図4 | パルス幅と角度の対応 | `fig4_pulse.svg` |
| 図5 | Pan-Tilt 接続（HO-6・**推奨**） | `fig5_pantilt_separate.svg` |
| 図5参考 | Pan-Tilt を Grove ハブで分岐する場合 | `fig5_pantilt.svg` |

すべて SVG なので、拡大しても劣化しません。手順書やスライドにそのまま貼れます。

---

## 9. トラブル時の切り分け

| 症状 | 第一容疑 |
|---|---|
| ボードが認識されない | ケーブルが充電専用 / IDE が繋ぎっぱなし |
| LED が光らない | `set_brightness()` が 0 になっていないか |
| 本体が熱い | 明るさを上げすぎ。12% に戻す |
| `i2c.scan()` が `[]` | PORT.B に挿している |
| **サーボで再起動する** | **Grove の赤線がブレッドボードに刺さっている** |
| **サーボが暴れる** | **共通 GND が繋がっていない** |
| サーボが無反応 | AC アダプタ未通電 / 信号が G36 側 |
| サーボが唸る | 可動範囲超過。すぐ止めて校正 |
| 画面が真っ黒 | `page0.screen_load()` を呼んでいない |
| 日本語が □ | `lv.AlibabaSans_JP24` 以外を指定している |
| Pan-Tilt が動かない | `set_freq(50)` を呼んでいない |
| バス1に何も出ない | PORT.C の SDA/SCL が逆。Pin(13)/Pin(14) を交換 |
| バス1で ETIMEDOUT | プルアップ不足、またはレベルシフタが必要 |
| **バス0まで壊れた** | **PORT.C に5Vバスを直結した可能性。要テスタ確認** |
