# 実機検証マニュアル

**目的**: HO-0 〜 HO-5 が期待どおり動くことを確認し、手順書 付録C の空欄を実測値で埋める。
**所要**: 2〜3時間（1台で通す場合）
**期限**: 2026年10月20日

---

## 0. 進め方

**A面（ブラウザー）とB面（ボード）を分けて確認し、最後に繋ぎます。** 最初から全部繋ぐと、どこで失敗したか分からなくなります。

```
Step 1  ブラウザー単体      check.html    ← ボード不要
Step 2  ボード単体        ho0_selftest  ← PC はターミナルのみ
Step 3  各ハンズオン      ho1〜ho3
Step 4  連結              ho4 + index.html
Step 5  AI 込み           HO-5
```

### GitHub Pages への移行について

**検証段階では localhost で構いません。** ローカルで一通り動くことを確認してから GitHub に移してください。

移行後に確認すべきことは1つだけです。**`https://〜.github.io/starter/` を開いて、`check.html` のセクション1がすべて OK になるか。** localhost と https はどちらもセキュアコンテキストなので、基本的にはそのまま動きます。

メンターへのハンズオン依頼は、移行後の URL で行ってください。**参加者と同じ経路で試してもらうこと**に意味があります。

### 準備するもの

- 検証用PC 1台（Chrome 148以上、Gemini Nano ダウンロード済み）
- Core2 for AWS 1台 + ENV Ⅲ + Grove ケーブル
- ブレッドボード組立済み 1セット + SG92R + USB AC アダプター
- USB-C データ通信対応ケーブル
- テスター（Pan-Tilt を使う場合のみ）

記録用に、このファイルをコピーして直接書き込んでいくのが楽です。

---

## Step 1. ブラウザー単体（30分）

ボードは繋ぎません。

### 1-1. ローカルサーバーを起動する

**Windows / PowerShell の場合**

```powershell
cd C:\starter
python -m http.server 8000 --bind 127.0.0.1
```

`python` で Microsoft Store が開いてしまう場合は `py` を使います。

```powershell
py -m http.server 8000 --bind 127.0.0.1
```

**macOS / Linux の場合**

```bash
cd starter
python3 -m http.server 8000 --bind 127.0.0.1
```

> **`--bind 127.0.0.1` を付けてください。** 省略すると社内ネットワークの他のPCからも見えてしまいます。ファイアウォールの警告も出なくなります。

停止は `Ctrl + C` です。

### 1-2. ページを開く

```
http://localhost:8000/check.html
```

> **`file://` では動きません。** Prompt API と Web Serial は https か localhost が必要です。

**検証中は DevTools（F12）を開き、Network タブの「Disable cache」にチェックを入れておいてください。** ファイルを書き換えたのに反映されない、という混乱を防げます。

### 1-3. board/*.py の使いかた

`board` の中の Python ファイルはサーバーからは使いません。UIFlow2 にコピー&ペーストするためのものです。

ブラウザーで `http://localhost:8000/board/ho1_blink.py` を開くとテキストが表示されるので、そこから全選択してコピーするのが早いです。

### 1-4. 「すべて実行」を押す

セクション1〜5が順に走ります。数分かかります。

### 1-5. 確認と記録

| セクション | 見るところ | 記録 |
|---|---|---|
| 1 ブラウザーと端末 | すべて OK か | Chrome バージョン: |
| 2 Gemini Nano | `available` か | availability: |
| 3 推論の速さ | 1回あたりの時間 | 平均: ____ ms |
| 4 スキーマ制約 | **制約なしの出力** | 下に貼る |
| 5 文脈 | 何回目で失敗するか | ____ 回目 |

**セクション4の「制約なし」の出力を必ず控えてください。**

```
（ここに貼る）


```

これが HO-5 で `responseConstraint` の必要性を説明する、最も説得力のある材料になります。AI が「はい、承知しました…」のような文章を返してきたら、それがそのまま教材です。

### 1-6. 機内モードで再確認

**PCを機内モード（またはWi-Fiオフ）にして、セクション3をもう一度実行してください。**

- [ ] オフラインでも推論が動いた

動けば、Day1 キックオフのデモがそのまま成立します。**ここが動かないと企画の柱が崩れる**ので、必ず確認してください。

---

## Step 2. ボード単体（40分）

PC はシリアルターミナルとしてのみ使います。

### 2-1. 自己診断を動かす

1. UIFlow2 Web IDE を開き、Core2 を接続
2. `board/ho0_selftest.py` の内容を貼り付け
3. 実行

ボタン A で次へ、B でやり直し、C で飛ばせます。

### 2-2. 6項目を順に確認

**[1] LED バー**
- [ ] 赤・緑・青・白がすべて点灯
- [ ] 1個ずつ流れる演出が動く
- [ ] 10個すべて光る（欠けがない）

**[2] 明るさと発熱** ← 重要
- [ ] 白100%で本体が再起動しない
- 発熱の程度: ________________
- 教材での推奨値を確定: ____ %

> 再起動するなら、それ自体が HO-1 の説明で使える実例です。動画に撮っておいてください。

**[3] I2C スキャン**
- `i2c.scan()` の実出力をそのまま記録:

```
（例: ['0x44', '0x70']）

```

- [ ] 0x44 が含まれる
- [ ] 0x70 が含まれる

> **ここが手順書の記述と食い違う可能性があります。** 実出力を正としてください。

**[4] 温湿度センサー**
- 表示値: ____ ℃ / ____ %
- 室温との差: ____ ℃
- [ ] **息を吹きかけて湿度が上がった**

> 最後の項目が肝心です。値が出ているだけではセンサーが動いている証拠になりません。

**[5] サーボ** ← 最重要
- [ ] 往復動作した
- [ ] **動作中に画面が消えない**（消えたら電源系のミス）
- [ ] 唸り音がしない

> 画面が消える場合、Grove ケーブルの赤線がブレッドボードに刺さっていないか真っ先に確認してください。

**[6] シリアル送信**
- [ ] ターミナルに10行の JSON が流れた

### 2-3. サーボの校正

自己診断とは別に、パルス幅の両端を探します。

```python
# REPL で直接叩く
servo_us(500)    # だんだん下げていく
servo_us(600)
servo_us(700)
```

- 実測 `PULSE_MIN_US`: ________
- 実測 `PULSE_MAX_US`: ________

> **端を超えると「ジー」と鳴ります。聞こえたらすぐ止めてください。**

### 2-4. UIFlow2 の API 名を控える

ブロックで作って生成された Python と、教材のコードが一致するか確認します。

| 確認項目 | 教材の記述 | 実際 |
|---|---|---|
| LED 初期化 | `RGB(io=25, n=10, type="SK6812")` | |
| 明るさ | `set_brightness(12)` | |
| 全点灯 | `fill_color(0x33ff33)` | |
| 1個指定 | `set_color(i, 0x0078ff)` | |
| ラベル文字 | `set_text(str('...'))` | |
| ラベル色 | `set_text_color(0xff3300, 255, 0)` | |
| 画面表示 | `page0.screen_load()` | |
| 日本語フォント | `lv.AlibabaSans_JP24` | |

**違っていたら実際のほうが正しいです。** 教材を書き換えます。

---

## Step 3. 各ハンズオン（40分）

`board/` の各ファイルを順に動かします。自己診断と重複する部分もありますが、**参加者が実際に触るコードそのもの**を通すことに意味があります。

### HO-1 `ho1_blink.py`

- [ ] 緑と消灯が0.5秒ごとに切り替わる
- [ ] 改造課題「1個ずつ流れる光」が動く
- [ ] 改造課題「ボタンA押下中のみ点灯」が動く
- 所要時間の見込み: ____ 分（予定20分）

### HO-2 `ho2_env.py`

- [ ] `scan()` の結果が出る
- [ ] 温湿度が1秒ごとに更新される
- [ ] 改造課題「LEDバーグラフ」が動く
- 所要時間の見込み: ____ 分（予定35分）

### HO-3 `ho3_servo.py`

- [ ] 画面に角度とパルス幅が出る
- [ ] LED バーが角度計として動く
- [ ] サーボが0〜180度を往復する
- [ ] 日本語（「パルス幅」「サーボ角度」）が正しく表示される
- 所要時間の見込み: ____ 分（予定30分）

> **日本語表示は必ず目視してください。** □ になっていたらフォント指定のミスです。

---

## Step 4. 連結（40分）

ここが山場です。

### 4-1. ボード側の準備

1. `board/ho4_bridge.py` を **`main.py` として保存**
2. ボードをリセット
3. **IDE の「Disconnect」を押す**

- [ ] リセット後、画面に「READY」「接続してください」が出る
- [ ] LED が緑に点灯する

### 4-2. 接続テスト（check.html のセクション6）

1. `check.html` を開く
2. セクション6の「接続」を押す
3. ポート一覧から Core2 を選ぶ

- [ ] 受信行数が増えていく
- [ ] JSON として解釈できている
- [ ] **接続直後にボードが再起動しない**

> 再起動する場合、`setSignals` が効いていません。`app.js` / `check.js` の `connect()` を確認してください。

4. 「テスト送信」を押す

- [ ] サーボが 0° → 90° → 180° → 90° と動く
- [ ] 画面に「GOOD」「テスト」が表示される
- [ ] LED バーが連動する

### 4-3. 本体ページ（index.html）

1. `check.html` を「切断」してから `index.html` を開く
2. 「ボードに接続」

- [ ] 温度・湿度が1秒ごとに更新される
- [ ] 波形（スコープ）が表示される
- [ ] スライダーを動かすとサーボが追従する
- [ ] 波形のパルス幅が角度に応じて変わる
- 所要時間の見込み: ____ 分（予定45分）

### 4-4. ポート競合の確認

**当日の質問No.1になる箇所です。挙動を把握しておいてください。**

1. `index.html` を接続したまま、IDE で Connect を試す
   - エラーメッセージ: ________________
2. IDE を接続したまま、ページで「接続」を試す
   - エラーメッセージ: ________________

このメッセージを手順書に書いておくと、参加者が自己解決できます。

---

## Step 5. AI 込み（30分）

`index.html` を接続した状態で行います。

### 5-1. 基本動作

- [ ] 「いま判断させる」でAIが応答する
- [ ] サーボがAIの返した角度に動く
- [ ] 画面に status（GOOD等）と日本語コメントが出る
- [ ] LED バーが必要度に応じて変わる

### 5-2. 推論モードの確認

| モード | 確認 |
|---|---|
| 手動 | [ ] ボタンを押したときだけ動く |
| 変化したとき | [ ] 息を吹きかけると発火する |
| 定期 | [ ] 30秒ごとに動く |
| 毎回 | [ ] 動くが PC が重くなる |

> 「毎回」で実際にどれくらい重くなるかを体感しておいてください。HO-5 の説明で「毎秒呼ばない設計」を語る根拠になります。

### 5-3. スキーマ制約を外す

「パラメーター」タブの「スキーマ制約を外す」にチェック → 「設定を反映」→「いま判断させる」

- [ ] `JSON.parse` が失敗し、ログにエラーが出る
- 実際に返ってきた文章: ________________

### 5-4. システムプロンプトの書き換え

「システムプロンプト」タブを別の役割に書き換えて「設定を反映」。

- [ ] コードを変えずに挙動が変わることを確認

### 5-5. 機内モードで通し

**最後に、機内モードにして5-1をもう一度実行してください。**

- [ ] オフラインでもすべて動く

- 所要時間の見込み: ____ 分（予定60分）

---

## Step 6. まとめ

### 6-1. 診断結果を出力

`check.html` のセクション7で「結果をまとめる」→「コピー」。付録C にそのまま貼れます。

### 6-2. タイムテーブルの検証

各ハンズオンの所要時間見込みを合計します。

| | 予定 | 見込み |
|---|---|---|
| HO-0 | 30分 | |
| HO-1 | 20分 | |
| HO-2 | 35分 | |
| HO-3 | 30分 | |
| HO-4 | 45分 | |
| **Day1 合計** | **160分** | |
| HO-5 | 60分 | |

> **運営が通す時間の1.5〜2倍**が参加者の所要時間の目安です。運営は答えを知っているためです。合計が Day1 の枠（13:30〜17:30、アイデアソン45分を含む）に収まらない場合、HO-1 をさらに削るか、HO-3 の改造課題を任意にしてください。

### 6-3. 教材の修正リスト

検証で見つかった不一致を書き出します。

| ファイル | 箇所 | 修正内容 |
|---|---|---|
| | | |
| | | |
| | | |

### 6-4. 撮影しておくもの

HO-1 と HO-3 の説明で使います。

- [ ] LED 白100%で再起動する様子（起きた場合）
- [ ] Grove 給電でサーボを動かして再起動する様子
- [ ] 共通GNDを外してサーボが暴れる様子
- [ ] 機内モードでAIが動く様子（デモの予備）

---

## つまずいたときの参照先

| 症状 | 参照 |
|---|---|
| Prompt API が使えない | check.html セクション1・2 |
| ボードが認識されない | ケーブル（データ通信対応か）、IDE の切断 |
| センサーが読めない | 自己診断 [3][4]、PORT.A か |
| サーボで再起動する | Grove の赤線、外部電源 |
| サーボが暴れる | 共通GND |
| 画面が真っ黒 | `page0.screen_load()` |
| 日本語が □ | `lv.AlibabaSans_JP24` を使っているか |
| AI が JSON を返さない | `responseConstraint` を渡しているか |
